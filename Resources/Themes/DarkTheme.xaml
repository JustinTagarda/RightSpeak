<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#0F172A" />
    <SolidColorBrush x:Key="WindowBorderBrush" Color="Transparent" />
    <SolidColorBrush x:Key="TitleBarBackgroundBrush" Color="Transparent" />
    <SolidColorBrush x:Key="TitleBarBorderBrush" Color="Transparent" />
    <SolidColorBrush x:Key="TitleBarForegroundBrush" Color="#E2E8F0" />
    <SolidColorBrush x:Key="TitleBarButtonHoverBackgroundBrush" Color="#20FFFFFF" />
    <SolidColorBrush x:Key="TitleBarButtonPressedBackgroundBrush" Color="#35FFFFFF" />
    <SolidColorBrush x:Key="TitleBarCloseButtonHoverBackgroundBrush" Color="#C42B1C" />
    <SolidColorBrush x:Key="TitleBarCloseButtonPressedBackgroundBrush" Color="#A72317" />
    <SolidColorBrush x:Key="TitleBarCloseButtonForegroundBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="WindowForegroundBrush" Color="#E2E8F0" />
    <SolidColorBrush x:Key="SubtleTextBrush" Color="#94A3B8" />
    <SolidColorBrush x:Key="FooterTextBrush" Color="#94A3B8" />
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="#111827" />
    <SolidColorBrush x:Key="CardBorderBrush" Color="#334155" />
    <SolidColorBrush x:Key="PrimaryButtonBackgroundBrush" Color="#3B82F6" />
    <SolidColorBrush x:Key="PrimaryButtonBorderBrush" Color="#2563EB" />
    <SolidColorBrush x:Key="PrimaryButtonForegroundBrush" Color="#F8FAFC" />
    <SolidColorBrush x:Key="SecondaryButtonBackgroundBrush" Color="#1F2937" />
    <SolidColorBrush x:Key="SecondaryButtonBorderBrush" Color="#475569" />
    <SolidColorBrush x:Key="SecondaryButtonForegroundBrush" Color="#E2E8F0" />
    <SolidColorBrush x:Key="DisabledButtonBackgroundBrush" Color="#1E293B" />
    <SolidColorBrush x:Key="DisabledButtonBorderBrush" Color="#334155" />
    <SolidColorBrush x:Key="DisabledButtonForegroundBrush" Color="#64748B" />
    <SolidColorBrush x:Key="InputBackgroundBrush" Color="#0B1220" />
    <SolidColorBrush x:Key="InputBorderBrush" Color="#334155" />
    <SolidColorBrush x:Key="InputForegroundBrush" Color="#E2E8F0" />
    <SolidColorBrush x:Key="ComboBoxBackgroundBrush" Color="#1F2937" />
    <SolidColorBrush x:Key="ComboBoxBorderBrush" Color="#475569" />
    <SolidColorBrush x:Key="ComboBoxForegroundBrush" Color="#E2E8F0" />
    <SolidColorBrush x:Key="StatusBackgroundBrush" Color="#13233F" />
    <SolidColorBrush x:Key="StatusBorderBrush" Color="#2C4A78" />
    <SolidColorBrush x:Key="StatusForegroundBrush" Color="#E2E8F0" />
    <SolidColorBrush x:Key="WarningTextBrush" Color="#FB923C" />
    <SolidColorBrush x:Key="HoverBorderBrush" Color="#7EB4EA" />
    <SolidColorBrush x:Key="SliderActiveBrush" Color="#3B82F6" />
    <SolidColorBrush x:Key="SliderInactiveBrush" Color="#64748B" />
    <SolidColorBrush x:Key="SliderThumbBrush" Color="#3B82F6" />
    <SolidColorBrush x:Key="SliderThumbBorderBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="ScrollBarTrackBrush" Color="#172136" />
    <SolidColorBrush x:Key="ScrollBarThumbBrush" Color="#475569" />
    <SolidColorBrush x:Key="ScrollBarThumbHoverBrush" Color="#60738C" />
    <SolidColorBrush x:Key="ScrollBarThumbPressedBrush" Color="#7B90AA" />

    <Style TargetType="ScrollViewer">
        <Setter Property="Padding" Value="0,0,1,0" />
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="#E2E8F0" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid VerticalAlignment="Center" SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="14" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border x:Name="OuterCircle"
                                Grid.Column="0"
                                Width="14"
                                Height="14"
                                CornerRadius="7"
                                BorderThickness="1"
                                BorderBrush="#64748B"
                                Background="Transparent" />

                        <Ellipse x:Name="InnerDot"
                                 Grid.Column="0"
                                 Width="6"
                                 Height="6"
                                 Fill="#3B82F6"
                                 Visibility="Collapsed"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center" />

                        <ContentPresenter Grid.Column="1"
                                          Margin="8,0,0,0"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="InnerDot" Property="Visibility" Value="Visible" />
                            <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#3B82F6" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#7EB4EA" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#64748B" />
                            <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#475569" />
                            <Setter TargetName="InnerDot" Property="Fill" Value="#64748B" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SliderThumbStyle" TargetType="Thumb">
        <Setter Property="Width" Value="14" />
        <Setter Property="Height" Value="14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Ellipse Fill="{DynamicResource SliderThumbBrush}"
                             Stroke="{DynamicResource SliderThumbBorderBrush}"
                             StrokeThickness="2"
                             SnapsToDevicePixels="True" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SliderTrackButtonStyle" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="2"
                            Height="5"
                            VerticalAlignment="Center" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Slider">
        <Setter Property="Height" Value="20" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid Margin="0,0,0,0" VerticalAlignment="Center">
                        <Border Height="5"
                                CornerRadius="2"
                                Background="{DynamicResource SliderInactiveBrush}"
                                VerticalAlignment="Center" />
                        <Track x:Name="PART_Track"
                               VerticalAlignment="Center"
                               IsDirectionReversed="{TemplateBinding IsDirectionReversed}"
                               Maximum="{TemplateBinding Maximum}"
                               Minimum="{TemplateBinding Minimum}"
                               Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Value, Mode=TwoWay}">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Style="{StaticResource SliderTrackButtonStyle}"
                                              Background="{DynamicResource SliderActiveBrush}" />
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource SliderThumbStyle}" />
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Style="{StaticResource SliderTrackButtonStyle}"
                                              Background="Transparent" />
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Padding" Value="14,6" />
        <Setter Property="Margin" Value="0,0,8,0" />
        <Setter Property="Background" Value="#3B82F6" />
        <Setter Property="Foreground" Value="#F8FAFC" />
        <Setter Property="BorderBrush" Value="#2563EB" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#7EB4EA" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="#1E293B" />
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#334155" />
                            <Setter Property="Foreground" Value="#64748B" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SecondaryButtonStyle" TargetType="Button">
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Padding" Value="14,6" />
        <Setter Property="Margin" Value="0,0,8,0" />
        <Setter Property="Background" Value="#1F2937" />
        <Setter Property="Foreground" Value="#E2E8F0" />
        <Setter Property="BorderBrush" Value="#475569" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#7EB4EA" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="#1E293B" />
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#334155" />
                            <Setter Property="Foreground" Value="#64748B" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ComboBoxToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InputComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Background" Value="#1F2937" />
        <Setter Property="BorderBrush" Value="#475569" />
        <Setter Property="Foreground" Value="#E2E8F0" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="6,4,8,4" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <ToggleButton x:Name="ToggleButton"
                                      Style="{StaticResource ComboBoxToggleButtonStyle}"
                                      Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"
                                      HorizontalContentAlignment="Stretch"
                                      VerticalContentAlignment="Stretch"
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Margin="{TemplateBinding Padding}"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Left"
                                           Text="{TemplateBinding SelectionBoxItem}"
                                           Foreground="{TemplateBinding Foreground}"
                                           TextAlignment="Left"
                                           TextTrimming="CharacterEllipsis" />
                                <Path Grid.Column="1"
                                      Margin="0,0,8,0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Right"
                                      Fill="{TemplateBinding Foreground}"
                                      Data="M 0 0 L 4 4 L 8 0 Z" />
                            </Grid>
                        </ToggleButton>
                        <Popup x:Name="Popup"
                               Placement="Bottom"
                               AllowsTransparency="True"
                               Focusable="False"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               PopupAnimation="Slide">
                            <Grid MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                  SnapsToDevicePixels="True">
                                <Border Background="#111827"
                                        BorderBrush="#334155"
                                        BorderThickness="1"
                                        CornerRadius="4" />
                                <ScrollViewer Margin="4"
                                              Padding="0,0,1,0"
                                              SnapsToDevicePixels="True">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsEnabled" Value="True" />
                    <Condition Property="IsMouseOver" Value="True" />
                </MultiTrigger.Conditions>
                <Setter Property="BorderBrush" Value="#7EB4EA" />
            </MultiTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
